window.addEventListener('DOMContentLoaded', () => {
    // Expose necessary Node.js APIs to the renderer process
    // For example:
    // const { ipcRenderer } = require('electron');
    // window.ipcRenderer = ipcRenderer;
  });